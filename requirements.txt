# Runtime dependencies
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-multipart>=0.0.9
numpy>=1.26,<2.0
opencv-python-headless>=4.9,<5.0
# Pinned: 0.10.14 still ships the bundled `solutions.pose` API (no separate model
# asset to manage). Newer 0.10.30+ builds expose only the Tasks API — migrating to
# `mediapipe.tasks` PoseLandmarker is tracked as future work.
mediapipe==0.10.14
anyio>=4.4
httpx>=0.27
supabase>=2.5,<3.0
